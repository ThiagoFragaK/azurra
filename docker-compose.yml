services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: devpassword
      POSTGRES_DB: azurra
    ports:
      - "5532:5432"
    volumes:
      - azurra_pgdata:/var/lib/postgresql/data

volumes:
  azurra_pgdata:
